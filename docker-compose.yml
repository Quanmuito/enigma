version: '3.8'

services:
  enigma-prod:
    container_name: enigma-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${NGINX_PORT:-8080}:80"
      - "${NGINX_PORT_SECURE:-8443}:443"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80/enigma"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - enigma-network

networks:
  enigma-network:
    driver: bridge